/**
 * Member management utility constants and helpers.
 * @module member-management-utils
 * @internal
 */

/** Email validation regex following RFC-5322 subset. */
export const EMAIL_REGEX = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
